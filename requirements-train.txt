# Training-only dependencies (install on Orchard compute node).
# Base pipeline deps are in requirements.txt.
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
evaluate>=0.4.0
jiwer>=3.0.0
tensorboard>=2.14.0
torch>=2.1.0
